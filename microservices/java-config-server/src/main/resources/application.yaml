server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/stron-jay/java-microservices-configurations
          clone-on-start: true
          default-label: main
